import { MoveableAreaDirective } from './moveable-area.directive';

describe('MoveableAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveableAreaDirective();
    expect(directive).toBeTruthy();
  });
});
