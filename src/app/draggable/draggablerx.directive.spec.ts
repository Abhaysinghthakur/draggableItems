import { DraggablerxDirective } from './draggablerx.directive';

describe('DraggablerxDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggablerxDirective();
    expect(directive).toBeTruthy();
  });
});
